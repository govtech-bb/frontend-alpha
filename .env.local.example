# Email Configuration
# Choose email provider: SMTP (local) or AWS SES (production)

# === SMTP Configuration (for local development) ===
# When SMTP_HOST is set, the application will use SMTP instead of AWS SES
# Recommended: Use MailHog or Mailpit for local email testing
#   MailHog: docker run -d -p 1025:1025 -p 8025:8025 mailhog/mailhog
#   Mailpit: docker run -d -p 1025:1025 -p 8025:8025 axllent/mailpit
# Then visit http://localhost:8025 to view sent emails

SMTP_HOST=""                    # e.g., "localhost" or "smtp.mailtrap.io"
SMTP_PORT="1025"                # Default for MailHog/Mailpit
SMTP_SECURE="false"             # Use "true" for port 465, "false" for other ports
SMTP_USER=""                    # Optional: username for authenticated SMTP
SMTP_PASS=""                    # Optional: password for authenticated SMTP
SMTP_WEB_PORT="8025"            # Optional: web interface port (for MailHog/Mailpit)

# === AWS SES Configuration (for production) ===
# When SMTP_HOST is not set, these settings are used
SES_REGION="us-east-1"

# === Common Email Configuration ===
# Verified sender email address
MAIL_FROM="noreply@gov.bb"

# Email addresses for form submissions
# For local testing, set these to your email address
FEEDBACK_TO_EMAIL="matt@dharach.com"
BIRTH_REGISTRATION_TO_EMAIL="matt@dharach.com"
PASSPORT_DEPARTMENT_EMAIL="matt@dharach.com"

# Production values:
# FEEDBACK_TO_EMAIL="feedback@gov.bb"
# BIRTH_REGISTRATION_TO_EMAIL="civil.registration@gov.bb"
# PASSPORT_DEPARTMENT_EMAIL="immigration@gov.bb"

# Optional: CloudWatch telemetry configuration for SES
SES_CONFIGURATION_SET=""
SES_TAG_KEY="ses:configuration-set"
SES_TAG_VALUE="prod"

# EZPay+ Payment Gateway Configuration
# Set to "true" to use mock payment gateway for local testing (no real API calls)
EZPAY_MOCK_MODE="true"

# API key provided by EZPay (request from payment gateway administrator)
# Only required when EZPAY_MOCK_MODE=false
EZPAY_API_KEY="your_ezpay_api_key_here"

# API URL - use test environment for development
# Only required when EZPAY_MOCK_MODE=false
EZPAY_API_URL="https://test.ezpay.gov.bb"
# Production: https://ezpay.gov.bb

# Payment code for passport replacement (configured in EZPay admin panel)
EZPAY_PASSPORT_CODE="PASSPORT001"
